server {
    server_name spark.ko4lax.dev;

    location / {
        proxy_pass http://127.0.0.1:3470;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/spark.ko4lax.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/spark.ko4lax.dev/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/options-ssl-nginx.conf;
}

server {
    if ($host = spark.ko4lax.dev) {
        return 301 https://$host$request_uri;
    }
    server_name spark.ko4lax.dev;
    listen 80;
    return 404;
}
