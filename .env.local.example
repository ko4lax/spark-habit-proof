# Copy to .env.local and set your deployed contract address.
# The app builds without this (falls back to zero address) but will not
# function until a real contract is deployed and this is set.
NEXT_PUBLIC_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
